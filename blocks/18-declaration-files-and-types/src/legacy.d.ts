// Type declarations for legacy.js (which has no types of its own).
// A .d.ts holds declarations only — no function bodies, no runtime code.
//
// TODO: declare the two exports of legacy.js so the imports in the tests are typed:
//   export function shout(message: string): string;
//   export const LANG: string;
export {};
