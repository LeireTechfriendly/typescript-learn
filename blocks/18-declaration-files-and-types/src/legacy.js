// An untyped JavaScript module (no types of its own).
// Your job: describe it in legacy.d.ts so the rest of the code is typed.

export function shout(message) {
  return message.toUpperCase() + "!";
}

export const LANG = "js";
